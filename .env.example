# ============================================
# OPENAI API - REQUERIDO
# ============================================
# Obtén tu API key en: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-TU-CLAVE-AQUI
# Modelo por defecto para las llamadas estándar (fallback: gpt-4o)
OPENAI_MODEL=gpt-4o

# Modelo alternativo para la variante Plus (fallback: OPENAI_MODEL)
OPENAI_MODEL_PLUS=gpt-4o

# ============================================
# GOOGLE API PARA VEO 3 - REQUERIDO
# ============================================
# Elige UNA de estas dos opciones:

# Opción A: Google Gemini API (Recomendado - Más simple)
# Obtén tu API key en: https://aistudio.google.com/app/apikey
GOOGLE_GEMINI_API_KEY=TU-CLAVE-GEMINI-AQUI
# Modelo base para prompts de texto (fallback: gemini-2.5-flash)
GEMINI_MODEL=gemini-2.5-flash
# Modelo rápido opcional (se usa el mismo si no se define)
GEMINI_TEXT_MODEL=gemini-2.5-flash

# Modelo para Vertex AI Gemini (fallback: gemini-2.5-pro)
VERTEX_GEMINI_MODEL=gemini-2.5-pro

# Modelo para generación de video con Veo 3.1 (fallback: veo-3.1-generate-preview)
VEO_VIDEO_MODEL=veo-3.1-generate-preview

# Opción B: Vertex AI con Service Account (Empresarial)
# Descomenta estas líneas si prefieres usar Vertex AI:
# GOOGLE_APPLICATION_CREDENTIALS=./service-account-key.json
# VERTEX_PROJECT_ID=tu-project-id
# VERTEX_LOCATION=us-central1

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================
PORT=3001
NODE_ENV=development

# ============================================
# RATE LIMITING
# ============================================
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=10